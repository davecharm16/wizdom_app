import 'package:flutter/material.dart';

class ClientModal extends StatelessWidget {
  const ClientModal({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Client Modal'),
      ),
    );
  }
}
